{
  "scripts": {
    "postinstall": "bower install --quiet",
    "test": "testem -f test/testem.json ci",
    "start": "testem -f test/testem.json",
    "stat": "cloc index.js --by-file --quiet && cloc test/*.js --by-file --quiet"
  },
  "devDependencies": {
    "bower": "^1.3",
    "testem": "^0.6"
  }
}
